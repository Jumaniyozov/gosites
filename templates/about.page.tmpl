{{template "base" .}}
{{define "title"}}
    About
{{end}}
{{define "content"}}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1>About Page</h1>

                <p>{{index .StringMap "remote_ip"}}</p>
            </div>
        </div>
    </div>
{{end}}
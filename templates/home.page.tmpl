{{template "base" .}}
{{define "title"}}
    Home
{{end}}
{{define "content"}}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1>Home Page</h1>
                <p>{{ index .StringMap "test"}}</p>
            </div>
        </div>
    </div>
{{end}}